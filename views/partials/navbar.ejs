<nav class="navbar navbar-expand-lg navbar-light bg-white  border-bottom shadow-sm" style="min-height:64px;">
    <div class="container-fluid">
        <a class="navbar-brand d-flex align-items-center fw-bold font-weight-bold" href="/">
            <i class="fas fa-heartbeat me-2 mr-2 text-primary"></i>
            <span class="text-dark">MediBook Clinic</span>
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav"
            data-toggle="collapse" data-target="#mainNav" aria-controls="mainNav" aria-expanded="false"
            aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="mainNav">
            <ul class="navbar-nav ms-auto ml-auto align-items-lg-center" style="display:flex;gap:8px;">
                <li class="nav-item"><a
                        class="nav-link <%= (typeof currentPage==='string' && (currentPage==='home' || currentPage==='index')) ? 'active' : '' %>"
                        href="/">Home</a></li>
            
                <li class="nav-item"><a
                        class="nav-link <%= (typeof currentPage==='string' && currentPage.includes('services')) ? 'active' : '' %>"
                        href="/services">Services</a></li>

                <% if (user) { %>
                    <% const _role=(user.role || user.roleName || '' ).toLowerCase(); %>
                        <li class="nav-item dropdown ms-lg-3">
                            <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="userMenu"
                                role="button" data-bs-toggle="dropdown" data-toggle="dropdown">
                                <div class="rounded-circle bg-gradient d-inline-flex align-items-center justify-content-center me-2 mr-2"
                                    style="width:36px;height:36px;color:white;background:linear-gradient(90deg,#2563eb,#1e40af);font-weight:700;">
                                    <%= (user.firstName || 'U' ).charAt(0).toUpperCase() %>
                                </div>
                                <span class="me-2 mr-2">
                                    <%= user.firstName %>
                                </span>
                                <span class="badge bg-primary badge-primary text-uppercase">
                                    <%= _role %>
                                </span>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end dropdown-menu-right">
                                <li><a class="dropdown-item" href="/<%= _role %>/dashboard"><i
                                            class="fas fa-tachometer-alt me-2 mr-2"></i>Dashboard</a></li>
                                <li><a class="dropdown-item" href="/profile"><i
                                            class="fas fa-user me-2 mr-2"></i>Profile</a>
                                </li>
                                <li>
                                    <hr class="dropdown-divider">
                                </li>
                                <li><a class="dropdown-item" href="/logout"><i
                                            class="fas fa-sign-out-alt me-2 mr-2"></i>Logout</a></li>
                            </ul>
                        </li>
                        <% } else { %>
                            <li class="nav-item ms-lg-2"><a class="nav-link" href="/login">Login</a></li>
                            <li class="nav-item ms-lg-2"><a class="nav-link" href="/register/patient">Register</a></li>
                            <% } %>
            </ul>
        </div>
    </div>
</nav>